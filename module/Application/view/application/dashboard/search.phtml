<div class="row">
    <form action="<?= $this->url('dashboard', ['action' => 'search']) ?>" class="async col-md-3 collapse" id="add-area-around">
        <div class="form-group">
            <label class="control-label application-label" for="area_value"><?= $this->translate('Set Area Around') ?>*</label>
            <input id="area_value" name="area_value" type="text" placeholder="km" class="form-control input-md application-input" required/>
        </div>
        <button>Create area</button>
    </form>
</div>
<a type="button" class="row" data-toggle="collapse" data-target="#add-area-around">+ Set area around</a>
<table class="table">
    <tr>
        <th>#</th>
        <th>Text Value</th>
        <th>Value in meters</th>
    </tr>
    <?php foreach ($this->paginator as $WH) : ?>
        <tr>
            <?php /** @var \Application\Model\Area $area */ ?>
            <td><?= $WH->getId() ?></td>

        </tr>
    <?php endforeach; ?>

</table>
<div class="text-right">
    <?= $this->render('layout/helper/pagination') ?>
</div>