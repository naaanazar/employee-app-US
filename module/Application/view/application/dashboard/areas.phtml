<form action="<?= $this->url('dashboard', ['action' => 'register-keys']) ?>" class="async">
    <input type="hidden" name="add" value="1">
    <button>Create area</button>
</form>
<table class="table">
    <tr>
        <th>#</th>
        <th>Text Value</th>
        <th>Value in seconds</th>
    </tr>
        <?php foreach ($this->paginator as $area) : ?>
            <tr>
                <?php /** @var \Application\Model\Area $area */ ?>
                <td><?= $area->getId() ?></td>
                <td><?= $area->getValue() ?></td>
                <td><?= $area->getIntValue() ?></td>
            </tr>
            <?php endforeach; ?>
</table>
<div class="text-right">
    <?= $this->render('layout/helper/pagination') ?>
</div>